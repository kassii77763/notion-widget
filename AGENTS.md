# Notion Multi-Widget プロジェクト規定 (AGENTS.md)

このファイルは、AIエージェント（Antigravity）がこのプロジェクト内でコードを書く際・提案する際に常に遵守するルールブックです。

## 1. デザインガイドライン
- **スタイルテーマ**: ボタニカル・ナチュラル（アースカラー＋目に優しいオーガニックシンプルUI）
- **カラーパレット**: 
  - 背景: 深いフォレストグリーン・アースグラデーション (`#182825` 〜 `#0d1715`)
  - ガラスカード: 落ち着いたセージマットグラス (`rgba(24, 40, 37, 0.75)`) + 薄い木漏れ日ボーダー
  - アクセント: セージグリーン (`#a3b18a`), ウォームゴールド/木漏れ日 (`#dda15e`), 柔らかいテラコッタ (`#bc6c25`)
  - 文字色: 目に優しい生成り・オフホワイト (`#f4f1de`, `#e9edc9`)
- **フォント**: Google Fonts (`Outfit`, `Inter`) を使用し、自然体で読みやすいデザインにする。

## 2. 動作・機能要求
- Notionの Embed（埋め込み）枠（小サイズ〜大サイズ）にきれいに収まるレスポンシブデザイン。
- タブ切り替えで以下の3モードを切り替え可能とする：
  1. **Clock Mode** (デジタル/アナログ表示切替、日付、曜日、秒数)
  2. **Pomodoro Mode** (25分作業 / 5分休憩カウントダウン、スタート・パウズ・リセット)
  3. **Countdown Mode** (指定日時までのカウントダウン、目標タイトル設定機能)
- ユーザーの設定状態（モード選択、タイマー設定、目標日付）は `localStorage` に保持する。

## 3. 技術仕様
- 外部フレームワークを使わない純粋な HTML5 / Vanilla CSS / JavaScript (ES6+)。
- サウンド通知（Web Audio APIによる優しいビープ音）。
